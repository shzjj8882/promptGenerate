# Docker Compose 环境变量配置
# 复制此文件并根据实际情况修改配置

# ============================================
# 数据库配置
# ============================================
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=aily_db

# ============================================
# Redis 配置
# ============================================
REDIS_PASSWORD=
REDIS_DB=0

# ============================================
# 服务端口配置
# ============================================
APP_PORT=3000

# ============================================
# 应用配置
# ============================================
DEBUG=False
ENV=production
SECRET_KEY=1234567890
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS 配置（多个域名用逗号分隔）
# 如果前端配置了域名，需要将域名添加到此处
# 例如：CORS_ORIGINS=http://localhost:3000,https://ada.championsea.online
CORS_ORIGINS=http://localhost:3000,https://ada.championsea.online

# DeepSeek API 配置
DEEPSEEK_API_KEY=sk-4a814e9aa1424c78b86f9af4cfbd8d46
DEEPSEEK_API_BASE=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# API Key（可选）
API_KEY=

# ============================================
# 前端配置
# ============================================
# 如果留空，前端将使用相对路径，通过 Next.js rewrites 代理后端请求
# 如果需要直接访问后端，可以设置为后端地址（如：http://localhost:8000）
NEXT_PUBLIC_API_BASE_URL=
