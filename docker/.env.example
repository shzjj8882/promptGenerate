# Docker Compose 环境变量示例
# 复制此文件为 .env 并修改相应配置

# ============================================
# 数据库配置
# ============================================
# 完整版（docker-compose.full.yml）：使用容器内的数据库，POSTGRES_HOST 和 REDIS_HOST 会自动设置为服务名
# 外置数据库版（docker-compose.yml）：需要配置外置数据库的主机地址

# PostgreSQL 配置
POSTGRES_HOST=localhost          # 外置数据库版：数据库主机地址（如：192.168.1.100 或 host.docker.internal）
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=aily_db

# Redis 配置
REDIS_HOST=localhost             # 外置数据库版：Redis 主机地址（如：192.168.1.100 或 host.docker.internal）
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ============================================
# 服务端口配置
# ============================================
SERVICE_PORT=8000
APP_PORT=3000

# Nginx 端口配置（仅用于 with-nginx 版本）
NGINX_PORT=80
NGINX_HTTPS_PORT=443

# ============================================
# 应用配置
# ============================================
DEBUG=False
ENV=production
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS 配置（多个域名用逗号分隔）
# 如果前端配置了域名，需要将域名添加到此处
# 例如：CORS_ORIGINS=http://localhost:3000,https://ada.championsea.online
CORS_ORIGINS=http://localhost:3000,https://ada.championsea.online

# DeepSeek API 配置
DEEPSEEK_API_KEY=your-api-key
DEEPSEEK_API_BASE=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# API Key（可选）
API_KEY=

# ============================================
# 前端配置
# ============================================
# Docker Nginx 模式：留空，使用相对路径（通过 Nginx 代理）
# 如果需要直接访问后端（不使用 Nginx），设置为：http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=
