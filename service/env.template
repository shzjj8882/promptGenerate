# 环境变量配置示例
# 复制此文件为 .env 并修改相应配置

# 应用配置
DEBUG=False
APP_NAME=AILY API
# 环境：本地开发不设置或设为 development；生产部署请设为 production（此时必须设置 SECRET_KEY）
# ENV=development

# PostgreSQL 配置
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=aily_db

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# CORS 配置（多个用逗号分隔）
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# JWT 配置（生产环境必须设置 SECRET_KEY 且不可使用下方默认值）
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 应用接口 /api 可选鉴权：设置后请求 /api 时需在请求头携带 X-API-Key: <API_KEY>；不设置则 /api 无需鉴权
# API_KEY=

# 数据库 schema 版本检查：设为 1 时，检测到结构变更将自动确认更新（适用于 CI/Docker 等非交互场景）
# AUTO_MIGRATE=1
